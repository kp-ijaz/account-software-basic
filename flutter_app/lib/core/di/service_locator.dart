import 'package:get_it/get_it.dart';

final getIt = GetIt.instance;

void setupServiceLocator() {
  // Repositories will be registered here in future phases
  // Services will be registered here in future phases
  // BLoCs will be registered here in future phases

  // For now, just initialize the service locator
  // Phase 3 will add database repositories
  // Phase 4 will add authentication services
  // Phase 5+ will add all other services
}
