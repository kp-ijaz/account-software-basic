/**
 * useAuthInit - Auth is initialized in main.tsx via onBeforeLift.
 * This hook is kept as a no-op for compatibility with existing imports.
 */

export const useAuthInit = () => {};

export default useAuthInit;
